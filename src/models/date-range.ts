export interface IDateRange {
  from_date: Date;
  to_date: Date;
}
export interface DateRangeSelection {
  fromDate: number;
  toDate: number;
  fromDateStr: string;
  toDateStr: string;
  dateDifference: number;
}
